"""
noteweave-bfts — Best-First Tree Search for automated ML experiments.

Standalone pip package. Runs on the client (VS Code extension / CLI).
LLM calls route through the NoteWeave backend via backend_noteweave.
"""

__version__ = "0.1.0"
